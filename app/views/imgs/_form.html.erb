<%= form_for(img) do |f| %>
  <% if img.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(img.errors.count, "error") %> prohibited this img from being saved:</h2>

      <ul>
      <% img.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :img_fname_user %>
    <%= f.text_area :img_fname_user %>
  </div>

  <div class="field">
    <%= f.label :img_fname_server %>
    <%= f.text_area :img_fname_server %>
  </div>

  <div class="field">
    <%= f.label :img_size %>
    <%= f.text_field :img_size %>
  </div>

  <div class="field">
    <%= f.label :img_path_root %>
    <%= f.text_area :img_path_root %>
  </div>

  <div class="field">
    <%= f.label :img_path_up %>
    <%= f.text_area :img_path_up %>
  </div>

  <div class="field">
    <%= f.label :img_path_dn %>
    <%= f.text_area :img_path_dn %>
  </div>

  <div class="field">
    <%= f.label :ad_id %>
    <%= f.text_field :ad_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
